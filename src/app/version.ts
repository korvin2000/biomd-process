/**
 * Kept in sync with `package.json` by hand.
 *
 * Importing the manifest instead would drag a JSON module into the build output
 * and make the path depend on where `dist` sits — not worth it for one string.
 */
export const APP_VERSION = '0.1.0';
export const APP_NAME = 'biomd';
